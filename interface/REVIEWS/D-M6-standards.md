# Review D — Standards (STAMMTISCH M6) / Spec (MOTOKO I1)

Reviewer: adversarial, evidence-bound. Probes: `uvx ruff@0.16.8 check interface/` → clean; targeted pytest (spine/boundaries/router/5 services/5 pilots/feeds collector) → 129 passed; one /tmp probe (never committed) re-driving the screens through multiple spine frames → 2 passed. MOTOKO: `make -C engine test-interface` → 179 passed (once, sanctioned).

## Standards (M6)

Worst issue: the agent's self-reported UI fixes (cursor re-anchoring, filter focus stealing) and the spine-live re-poll itself are real in code but pinned by no shipped test — the README states them as contract.

1. **Unpinned self-reported fixes.** README:121 "Table cursors survive every repaint" — every `_deliver` re-anchors (screens/feeds_health.py:140,173-176; ledger.py:116-117,137-140; energy.py:157,174-178; polymarket.py:176,189-192), but no pilot delivers a second frame with a mid-table cursor (all await `frames_landed == 1`; the overview wave had test_overview_pilot.py:59). The focus latch (screens/polymarket.py:161-166, "later slow-lane re-deliveries never steal focus back") and the spine-live wiring ("on every services-due they re-fetch", README:117-119) are likewise unpinned. My /tmp probe (3 frames, cursor row 2 held; filter kept focus) confirms the behavior holds today — this is a missing-pin finding, not a bug.
2. **Rule (e) enforcement hole.** README:41-46 claims static enforcement; test_boundaries.py:221-233 only inspects Import/ImportFrom nodes that are *direct* children of `tree.body`. The comment at test_boundaries.py:218-220 ("ONLY direct children of the module body execute at import time") is false — imports under a top-level `if`/`try` also run at import time and would escape. Current screens are clean (only TYPE_CHECKING typing imports, screens/feeds_health.py:33-34), so the rule is true in fact; the guard is weaker than its letter.
3. **Duplicated Code — the egress twins.** `as_http_proxy`/`Egress`/`resolve_egress`/`_PinnedProxyHandler`/`_open`/`_proxy_request_error` are near-verbatim across services/energy.py:194-276,411-424 and services/polymarket.py:72-163,260-273 (~110 lines; only env var + UA differ), self-admitted "kept in sync with the energy twin" (energy.py:197-198). These twins are new code, not old-tree citations — the copy+adapt methodology does not force them. One `services/egress.py` parameterized by env-var name would delete the shotgun-surgery risk.
4. **Screen-shape duplication (partial).** The staleness-badge block is triplicated (feeds_health.py:112-122 / energy.py:110-120 / polymarket.py:126-136, only widget id + title differ); `action_refresh` in-flight guard and j/k cursor actions are quintuplicated. A small base/mixin covers these; the rest of the variance is real (ledger's two tables, sentiment's day nav, polymarket's filter) — full parameterization would be overreach.
5. **SlowLane vs SingleFlight — honest reuse.** SlowLane composes SingleFlight (app/spine.py:184) and adds generation + landing guard; tests cover never-overlap, drop-stale/unmounted, raising work, failed dispatch (test_spine.py:394-461). No duplication found.
6. **Verified claims.** Ledger injectable quote fn is honest: lazy default (services/ledger.py:190-194), raising leg degrades to no-marks (:217-220), pilots patch `interface.services.feeds.fetch_batch` through the lazy seam (test_ledger_pilot.py:68-69) and service tests inject `quotes=` (test_services_ledger.py:63,119,133). Sentiment direction pinned (test_sentiment_pilot.py:88-101: ← older, → newer, edges notify). Polymarket Enter is not a no-op — it shows detail; the read-only surface is documented via "Read-only market data · no order path" (services/polymarket.py:358, pinned test_polymarket_pilot.py:98). All tokens resolve in tokens.py; no credentials (proxy creds rejected, energy key env-only); English throughout (Chinese sentiment vocab is cited fixture data); ruff clean.
7. **Minor.** `market_url` (services/polymarket.py:64-69) is exported and tested but used by no screen — leftover surface from the removed browser launch.

## Spec (I1)

Worst issue: none material — all five brief items are delivered or correctly deferred; only borderline honesty nuances below.

1. **Provenance stamps — complete.** Brief: "additive `served_by: Mapping[str,str] = {}`… constructor-compat" → snapshot.py adds `served_by: Mapping[str, str] = field(default_factory=dict)` with the additive comment. "session stamps real sources ONLY when they served (keys absent when degraded)" → `_served_by` stamps engagements/events always on a valid root and gates/reports only when `gates is not None` (collectors/__init__.py:337-355); `gates_refresh` degrades gates and reports together, so the pairing is honest. "demo labels synthetic" → DEMO_SERVED_BY "demo synthetic" (demo.py:104-113). Rendered: topbar `src:` (render/panels.py:201-215,238-241, shared by app and watch — watch.py:283), status note (status.py:151-165), GATES border subtitle with clear-on-unavailable (app.py:1442-1450). Pinned incl. additive-only and not-shared-between-frames (test_provenance.py:27,53).
2. **Adapter reap — complete.** Brief: "module-level child registry (pid→weakref owner+Popen)" → adapter.py `_children` dict. "daemon watchdog reaps GC'd-owner spawns" → `_watchdog_loop`, pinned by test_adapter_lifecycle.py:112-126. "atexit `_reap_all()` bounded 2s/child never raises" → REAP_WAIT_S=2.0, `_stop_child` swallows OSError/Timeout. "`_teardown` refactored onto shared `_stop_child` with unchanged close-pipes-first" → diff preserves the pipes-before-terminate comment and order. "kill -9 residual gap documented" → module docstring (SIGKILL/PR_SET_PDEATHSIG reasoning). "close_sessions covered by a test" → test_adapter_lifecycle.py:129-143. "end-to-end proof with a stubborn adapter ignoring stdin EOF" → STUBBORN_ADAPTER sleeps 120s past EOF; a leaky driver exits without close; pid verified dead (test_adapter_lifecycle.py:95-109).
3. **Soak — complete.** Brief: "each event read exactly once — instrumented" → counting `_events_tail` wrapper, rows == total events, calls == hours (test_soak.py:87-96,118-123). "tail capped" → `max_tail <= TAIL_SIZE == 200` and saturated (:124-125). "staleness escalates >600s and recovers" → heartbeat ages past the threshold mid-soak, beat at half-time recovers (:132-137). "no unbounded dict growth" → cursors/tails/stuck/canary and app containers bounded (:127-131,175-180). "bell once per transition incl. the deliberate recovery ring now pinned as contract" → bells 1/2/3 across episodes with the "locked contract" comment (:183-199).
4. **Per-key generations — correctly not ported.** Brief: "port ONLY if parallel legs exist (agent claims none — verify)". Verified: watch.py:186-191 refuses to start a second worker ("cursor/tail state is not safe for overlapping reads"); app.py:1082-1086 keeps "at most one provider active". No parallel legs exist; porting would be speculative generality.
5. **Constraints held.** Only `engine/interface/**` in the uncommitted diff (git status); no engine commands; no signals (the only `kill` is the child-handle `proc.kill()` inside `_stop_child`).

Borderline nuances (no action demanded): `_served_by` stamps `events=ro-sqlite` even on frames whose per-engagement graph reads errored (collector_error set) — the collector ran, but "served" is arguable; soak demonstrates escalation at 3600s, not at the 600s boundary itself (the threshold is pre-existing, not I1 scope).

## Ranked minimal fixes

1. Add pilot pins: cursor-survives-repaint, polymarket focus latch, and a second spine-driven frame per spine-live screen (extend the existing pilots; my /tmp probe is the blueprint).
2. Fix the rule (e) walker: flag `interface.services` imports nested under any top-level `if`/`try` that is not `TYPE_CHECKING`, and correct the false docstring (test_boundaries.py:218-233).
3. Extract `services/egress.py` shared by the energy/polymarket twins (env-var name as parameter) — removes ~110 duplicated lines and the "kept in sync" liability.
4. Extract the triplicated staleness-badge block (+ refresh-guard/cursor actions) into a small screen base or mixin.
5. Drop or use `market_url` (services/polymarket.py:64-69).
